// Create `index` module that exports a function to kick things off
'use strict';

import ApplicationView from 'application';

export default function() {
  const newPuppytest = new ApplicationView;
}
